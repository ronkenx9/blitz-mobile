// Re-export the official Solana Mobile wallet hook.
// All components should use this import path so we have a single
// place to swap implementations if needed.
export { useMobileWallet } from '@wallet-ui/react-native-web3js';
